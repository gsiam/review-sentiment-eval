# Diagram sources

Mermaid sources in this directory are the canonical diagram definitions. Render SVG assets into `docs/images/` before updating README image links. `mermaid-config.json` controls the shared render theme.

```bash
scripts/render_diagrams.sh
```
